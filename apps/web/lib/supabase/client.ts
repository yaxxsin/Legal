// Supabase client has been removed.
// Auth is now handled via custom JWT + NestJS API.
// This file is kept as a placeholder to prevent import errors.
// It will be deleted once all references are verified clean.

export function createClient() {
  throw new Error(
    'Supabase client has been removed. Use NestJS API with JWT auth instead.',
  );
}
